# Dev server proxies /api to this target (see vite.config.ts)
VITE_API_TARGET=http://localhost:4000

# Optional: bypass the proxy and call the API directly (must be CORS-allowed by the backend)
# VITE_API_BASE_URL=http://localhost:4000/api
